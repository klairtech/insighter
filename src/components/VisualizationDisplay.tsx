"use client";

// Temporarily disabled VisualizationDisplay component to prevent runtime errors
// This component was causing issues and is no longer used in the chat client

export default function VisualizationDisplay() {
  return null;
}
